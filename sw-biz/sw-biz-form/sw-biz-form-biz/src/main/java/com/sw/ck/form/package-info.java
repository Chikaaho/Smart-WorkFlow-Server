/**
 * 低代码表单模块 — 表单定义、动态宽表、提交与事件发布。
 * <p>
 * 包含章节：
 * <ul>
 *   <li>{@code entity} — FormBaseEntity(UUID 体系) + 四张元数据表实体</li>
 *   <li>{@code dynamic} — DynamicTableManager 运行时 DDL 管理</li>
 *   <li>{@code mapper} — MyBatis-Plus Mapper 接口</li>
 *   <li>{@code service} — 表单定义 CRUD + 发布状态机</li>
 *   <li>{@code controller} — REST 接口</li>
 * </ul>
 * </p>
 */
package com.sw.ck.form;
